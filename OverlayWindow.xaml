    <Window x:Class="GameLauncher.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:GameLauncher.Services.Localization"
        Title="{loc:Loc Key=Overlay.Title}" 
        Height="165" Width="250"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        IsHitTestVisible="False"
        WindowStartupLocation="Manual">
    
    <Border Background="#CC1E1E1E" CornerRadius="8" Padding="15" BorderBrush="#33FFFFFF" BorderThickness="1">
        <StackPanel>
            <Grid Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{loc:Loc Key=Overlay.Cpu}" Foreground="White" FontSize="14"/>
                <TextBlock x:Name="CpuText" Grid.Column="1" Text="--°C | --%" Foreground="{DynamicResource AccentColor}" FontSize="14" FontWeight="SemiBold"/>
            </Grid>

            <Grid Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{loc:Loc Key=Overlay.Gpu}" Foreground="White" FontSize="14"/>
                <TextBlock x:Name="GpuText" Grid.Column="1" Text="--°C | --%" Foreground="{DynamicResource AccentColor}" FontSize="14" FontWeight="SemiBold"/>
            </Grid>

            <Grid Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{loc:Loc Key=Overlay.Ram}" Foreground="White" FontSize="14"/>
                <TextBlock x:Name="RamText" Grid.Column="1" Text="-- GB | --%" Foreground="{DynamicResource AccentColor}" FontSize="14" FontWeight="SemiBold"/>
            </Grid>

            <Grid Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{loc:Loc Key=Overlay.Vram}" Foreground="White" FontSize="14"/>
                <TextBlock x:Name="VramText" Grid.Column="1" Text="-- GB | --%" Foreground="{DynamicResource AccentColor}" FontSize="14" FontWeight="SemiBold"/>
            </Grid>

            <Grid Margin="0,5,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock x:Name="ActiveGameText" Text="{loc:Loc Key=Overlay.NoActiveGame}" Foreground="White" FontSize="11" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
                    <TextBlock x:Name="ClockText" Text="00:00" Foreground="#AAAAAA" FontSize="11"/>
                </StackPanel>
                <TextBlock x:Name="PlayTimeText" Grid.Column="1" Text="00:00:00" Foreground="#AAAAAA" FontSize="14" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>
