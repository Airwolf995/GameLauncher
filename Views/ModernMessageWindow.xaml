    <Window x:Class="GameLauncher.ModernMessageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="clr-namespace:GameLauncher.Services.Localization"
        Title="{loc:Loc Key=AppName}" Height="200" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.85"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Opacity" Value="0.7"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#333333"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#555555"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{DynamicResource AccentColor}" BorderThickness="2" CornerRadius="10">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock x:Name="TitleText" 
                      Grid.Row="0"
                      Text="{loc:Loc Key=AppName}" 
                      FontSize="18" 
                      FontWeight="Bold" 
                      Foreground="White"
                      Margin="0,0,0,15"/>

            <!-- Message -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="MessageText" 
                          Text="{loc:Loc Key=Common.Loading}" 
                          FontSize="14" 
                          Foreground="#CCCCCC"
                          TextWrapping="Wrap"
                          HorizontalAlignment="Left"
                          VerticalAlignment="Center"/>
            </ScrollViewer>

            <!-- Buttons -->
            <StackPanel x:Name="ButtonPanel" 
                       Grid.Row="2" 
                       Orientation="Horizontal" 
                       HorizontalAlignment="Right"
                       Margin="0,20,0,0">
                <Button x:Name="BtnYes" 
                       Content="{loc:Loc Key=Common.Yes}" 
                       Style="{StaticResource ModernButton}" 
                       Click="BtnYes_Click" 
                       Margin="0,0,10,0"
                       Visibility="Collapsed"/>
                <Button x:Name="BtnNo" 
                       Content="{loc:Loc Key=Common.No}" 
                       Style="{StaticResource SecondaryButton}" 
                       Click="BtnNo_Click"
                       Visibility="Collapsed"/>
                <Button x:Name="BtnOk" 
                       Content="{loc:Loc Key=Common.Ok}" 
                       Style="{StaticResource ModernButton}" 
                       Click="BtnOk_Click"
                       Visibility="Collapsed"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
